import { BookNotificationPayload } from './types';
export type EmailTheme = 'light' | 'dark' | 'auto';
export interface BookNotificationEmailOptions {
    /** Full URL to the light-theme logo image. If omitted, the logo is embedded (data URI) so it shows when HTML is pasted or viewed offline. */
    logoLightUrl?: string;
    /** Full URL to the dark-theme logo image (optional). */
    logoDarkUrl?: string;
    /**
     * Theme for the generated HTML.
     * - 'light': Always light mode (no dark media query). Use when pasting into another viewer to see light version.
     * - 'dark': Always dark mode (dark tokens as default).
     * - 'auto': Light by default; dark when user prefers (prefers-color-scheme: dark).
     */
    theme?: EmailTheme;
}
/**
 * Returns the full HTML for the book notification email (inline styles for email clients).
 * Logo is embedded by default so it shows when pasted. Use options.theme = 'light' to get light-only HTML.
 */
export declare function getBookNotificationEmailHtml(payload: BookNotificationPayload, options?: BookNotificationEmailOptions): string;
/**
 * Returns the recommended subject line for the book notification email.
 */
export declare function getBookNotificationSubject(payload: BookNotificationPayload): string;
