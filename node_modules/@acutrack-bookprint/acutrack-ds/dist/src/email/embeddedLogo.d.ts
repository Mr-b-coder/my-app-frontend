/** Data URI for the light logo (SVG). Use in img src so the logo shows when HTML is pasted or viewed offline. */
export declare const EMBEDDED_LOGO_LIGHT_DATA_URI: string;
/** Data URI for the dark logo (SVG). Use in auto mode so dark theme shows the correct logo when pasted. */
export declare const EMBEDDED_LOGO_DARK_DATA_URI: string;
