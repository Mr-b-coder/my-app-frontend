import { default as React } from 'react';
import { BookNotificationPayload } from './types';
export interface BookNotificationEmailProps {
    payload: BookNotificationPayload;
    /** Optional class for the wrapper */
    className?: string;
}
export declare const BookNotificationEmail: React.FC<BookNotificationEmailProps>;
