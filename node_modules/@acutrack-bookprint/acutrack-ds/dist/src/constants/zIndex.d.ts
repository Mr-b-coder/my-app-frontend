/**
 * Design system z-index scale.
 * Use these values so overlays, modals, and sticky UI stack consistently.
 *
 * Order (low to high):
 * - base: in-flow stacking (e.g. calendar grid, tab indicator)
 * - sticky: sticky headers/footers
 * - modal: modal overlay and content
 * - sidebarBackdrop: sidebar overlay backdrop
 * - sidebar: sidebar overlay panel (above modal when overlay is open)
 * - toast: toast container (below overlays so dropdowns/popovers can appear above toasts)
 * - overlay: all floating UI (popover, dropdown, multiselect, autocomplete, tooltip, drawer)
 *
 * Form components with overlays (Select, MultiSelect, Autocomplete, DatePicker, DateRangePicker)
 * get overlay z-index from Dropdown or Popover — no extra props needed.
 */
export declare const Z_INDEX: {
    readonly base: 1;
    readonly sticky: 40;
    readonly modal: 50;
    readonly sidebarBackdrop: 59;
    readonly sidebar: 60;
    readonly toast: 9998;
    readonly overlay: 9999;
};
/** Tailwind-compatible class for overlay layer (popover, dropdown, tooltip, etc.) */
export declare const Z_INDEX_OVERLAY_CLASS = "z-[9999]";
/** Tailwind-compatible class for toast layer */
export declare const Z_INDEX_TOAST_CLASS = "z-[9998]";
/** Tailwind-compatible class for modal layer */
export declare const Z_INDEX_MODAL_CLASS = "z-50";
/** Tailwind-compatible class for sidebar overlay panel */
export declare const Z_INDEX_SIDEBAR_CLASS = "z-[60]";
/** Tailwind-compatible class for sidebar backdrop */
export declare const Z_INDEX_SIDEBAR_BACKDROP_CLASS = "z-[59]";
/** Tailwind-compatible class for sticky content */
export declare const Z_INDEX_STICKY_CLASS = "z-40";
