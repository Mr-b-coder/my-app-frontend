import { default as React } from 'react';
/**
 * Drawer position options
 */
export type DrawerPosition = 'left' | 'right' | 'top' | 'bottom';
/**
 * Drawer size options
 */
export type DrawerSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
/**
 * Props for the Drawer.Root component
 */
export interface DrawerRootProps {
    /** Whether the drawer is open */
    isOpen: boolean;
    /** Callback when the drawer should close */
    onClose: () => void;
    /** The content to display in the drawer */
    children: React.ReactNode;
    /** The position of the drawer */
    position?: DrawerPosition;
    /**
     * The size of the drawer. Uses a fixed scale (Tailwind-default rem) so xs through xl progress correctly
     * regardless of theme overrides. Override with `className` (e.g. `!max-w-sm`) for theme-based dimensions.
     * Set `useThemeSizing={true}` to use your theme's max-w-* / max-h-* instead.
     */
    size?: DrawerSize;
    /**
     * When true, width (left/right) and height (top/bottom) use your theme's max-w-* and max-h-* classes.
     * Default false uses a fixed scale for consistent progression. Use for backward compatibility if you
     * relied on theme overrides.
     */
    useThemeSizing?: boolean;
    /** Custom CSS class name; applied last so it can override size/position. */
    className?: string;
    /** Custom CSS styles */
    style?: React.CSSProperties;
    /** Whether to show backdrop */
    hasBackdrop?: boolean;
    /** Whether to close on backdrop click */
    closeOnBackdropClick?: boolean;
}
/**
 * Props for the Drawer.Header component
 */
export interface DrawerHeaderProps {
    /** The content to display in the header */
    children: React.ReactNode;
    /** Custom CSS class name */
    className?: string;
    /** Custom CSS styles */
    style?: React.CSSProperties;
    /** Whether to show close button */
    hasCloseButton?: boolean;
}
/**
 * Props for the Drawer.Body component
 */
export interface DrawerBodyProps {
    /** The content to display in the body */
    children: React.ReactNode;
    /** Custom CSS class name */
    className?: string;
    /** Custom CSS styles */
    style?: React.CSSProperties;
}
/**
 * Props for the Drawer.Footer component
 */
export interface DrawerFooterProps {
    /** The content to display in the footer */
    children: React.ReactNode;
    /** Custom CSS class name */
    className?: string;
    /** Custom CSS styles */
    style?: React.CSSProperties;
}
export declare const Drawer: React.FC<DrawerRootProps> & {
    Header: React.FC<DrawerHeaderProps>;
    Body: React.FC<DrawerBodyProps>;
    Footer: React.FC<DrawerFooterProps>;
};
