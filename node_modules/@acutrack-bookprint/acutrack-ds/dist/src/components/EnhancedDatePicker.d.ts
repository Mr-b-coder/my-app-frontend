import { default as React } from 'react';
import { PopoverPosition } from './Popover';
export type DatePickerVariant = 'single' | 'range';
export type EnhancedDatePickerAlignment = 'left' | 'center' | 'right';
export interface DateRange {
    start: Date | null;
    end: Date | null;
}
export interface DatePickerProps {
    /** The type of date picker to render */
    variant?: DatePickerVariant;
    /** The selected date or date range. Controlled component value. */
    value: Date | DateRange | null;
    /** Callback fired when the date value changes */
    onChange: (date: Date | DateRange | null) => void;
    /** The earliest date that can be selected */
    minDate?: Date;
    /** The latest date that can be selected */
    maxDate?: Date;
    /** A function to disable specific dates */
    isDateDisabled?: (date: Date) => boolean;
    /** Placeholder text for the input field */
    placeholder?: string;
    /** Disables the date picker */
    isDisabled?: boolean;
    /** Custom class name for the root element */
    className?: string;
    /** Whether to close the popover after selection */
    closeOnSelect?: boolean;
    /** Whether to show the today button */
    showTodayButton?: boolean;
    /** Whether to show the clear button */
    showClearButton?: boolean;
    /** Custom format for displaying dates */
    dateFormat?: string;
    /** First day of the week (0 = Sunday, 1 = Monday) */
    firstDayOfWeek?: 0 | 1;
    /** The position of the popover relative to the trigger */
    position?: PopoverPosition;
    /** Text alignment for the label and input field */
    align?: EnhancedDatePickerAlignment;
    /** The border radius of the date picker */
    radius?: 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
}
export declare const EnhancedDatePicker: React.FC<DatePickerProps>;
export default EnhancedDatePicker;
