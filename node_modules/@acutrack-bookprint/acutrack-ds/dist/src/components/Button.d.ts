import { default as React } from 'react';
type AsProp<C extends React.ElementType> = {
    /** The HTML element or React component to render as */
    as?: C;
};
type PropsToOmit<C extends React.ElementType, P> = keyof (AsProp<C> & P);
type PolymorphicComponentProps<C extends React.ElementType, P = {}> = React.PropsWithChildren<P & AsProp<C>> & Omit<React.ComponentPropsWithoutRef<C>, PropsToOmit<C, P>>;
type PolymorphicRef<C extends React.ElementType> = React.ComponentPropsWithRef<C>['ref'];
/**
 * Props for the Button component
 */
interface ButtonOwnProps {
    /** The content to display inside the button */
    children?: React.ReactNode;
    /** Additional CSS classes to apply to the button */
    className?: string;
    /** The visual style variant of the button */
    variant?: 'primary' | 'secondary' | 'text' | 'ghost' | 'outline' | 'dashed' | 'warning' | 'danger' | 'info' | 'success' | 'grey-primary' | 'grey-secondary' | 'grey-text' | 'grey-ghost' | 'grey-outline' | 'grey-dashed';
    /** The size of the button */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
    /** Icon to display on the left side of the button content */
    leftIcon?: React.ReactNode;
    /** Icon to display on the right side of the button content */
    rightIcon?: React.ReactNode;
    /** Whether the button is displayed on a dark background */
    onDarkBackground?: boolean;
    /** Whether the button is displayed on a brand-colored background */
    onBrandBackground?: boolean;
    /** Whether the button is disabled */
    isDisabled?: boolean;
    /** Whether the button is in a loading state */
    isLoading?: boolean;
    /** Whether the button should take full width of its container */
    isFullWidth?: boolean;
    /** Width of the button (preset options) */
    width?: 'sm' | 'md' | 'lg' | 'full' | 'auto' | 'none';
    /** Custom width value (e.g., "200px", "15rem", "50%"). Overrides width prop. */
    customWidth?: string;
    /** Minimum width override */
    minWidth?: string;
    /** Maximum width override */
    maxWidth?: string;
    /** Whether the button has hover effects (default: true) */
    hasHoverEffect?: boolean;
    /** Whether the button has active effects */
    hasActiveEffect?: boolean;
    /** The border radius of the button */
    radius?: 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
}
type ButtonProps<C extends React.ElementType> = PolymorphicComponentProps<C, ButtonOwnProps>;
declare const Button: <C extends React.ElementType = "button">(props: ButtonProps<C> & {
    ref?: PolymorphicRef<C>;
}) => React.ReactElement | null;
export { Button };
