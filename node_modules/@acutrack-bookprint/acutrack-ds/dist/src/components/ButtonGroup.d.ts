import { default as React } from 'react';
export interface ButtonGroupProps {
    /** The children buttons to group together */
    children: React.ReactNode;
    /** The orientation of the button group */
    orientation?: 'horizontal' | 'vertical';
    /** Whether the buttons are attached (no gap between them) */
    isAttached?: boolean;
    /** The size of all buttons in the group */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
    /** Additional CSS classes to apply to the button group */
    className?: string;
    /** Whether the group is disabled (disables all buttons) */
    isDisabled?: boolean;
    /** Whether the group is full width */
    isFullWidth?: boolean;
}
export declare const ButtonGroup: React.FC<ButtonGroupProps>;
export default ButtonGroup;
