import { default as React } from 'react';
import { SearchSuggestion } from './SearchBar';
/**
 * Props for the ExpandableSearchButton component
 */
export interface ExpandableSearchButtonProps {
    /** The placeholder text for the search input */
    placeholder?: string;
    /** The current search query value */
    value?: string;
    /** Callback function when search query changes */
    onChange?: (query: string) => void;
    /** Callback function when search is submitted */
    onSearch?: (query: string) => void;
    /** Callback function when search is cleared */
    onClear?: () => void;
    /** Callback function when search suggestions are requested */
    onSuggestionsRequest?: (query: string) => void;
    /** Callback function when a suggestion is selected */
    onSuggestionSelect?: (suggestion: SearchSuggestion) => void;
    /** Additional CSS classes for the button */
    className?: string;
    /** The size of the button */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
    /** The variant of the button */
    variant?: 'primary' | 'secondary' | 'text' | 'ghost' | 'outline';
    /** Whether the search is expanded */
    isExpanded?: boolean;
    /** Callback function when expand state changes */
    onExpandChange?: (expanded: boolean) => void;
    /** Whether to show search suggestions */
    hasSuggestions?: boolean;
    /** Array of search suggestions to display */
    suggestions?: SearchSuggestion[];
    /** Maximum number of suggestions to display */
    maxSuggestions?: number;
    /** Debounce delay for search input (in milliseconds) */
    debounceDelay?: number;
    /** Whether to show a clear button */
    hasClearButton?: boolean;
    /** Whether to show a voice search button */
    hasVoiceSearch?: boolean;
    /** Whether to show search filters */
    hasFilters?: boolean;
    /** Search filter options */
    filters?: Array<{
        id: string;
        label: string;
        value: string;
        isActive?: boolean;
    }>;
    /** Callback function when filters change */
    onFiltersChange?: (filters: string[]) => void;
    /** Whether the button should take full width of its container */
    isFullWidth?: boolean;
    /** Whether to show the search icon */
    showSearchIcon?: boolean;
    /** Custom search icon */
    searchIcon?: string;
    /** Whether to show loading spinner */
    showLoadingSpinner?: boolean;
    /** Whether to show search results count */
    showResultsCount?: boolean;
    /** Total number of search results */
    resultsCount?: number;
    /** Whether the button is disabled */
    isDisabled?: boolean;
    /** Whether the button is in a loading state */
    isLoading?: boolean;
}
export declare const ExpandableSearchButton: React.FC<ExpandableSearchButtonProps>;
export default ExpandableSearchButton;
