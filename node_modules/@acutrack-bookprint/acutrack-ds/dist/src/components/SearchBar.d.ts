import { default as React } from 'react';
export interface SearchSuggestion {
    id: string;
    text: string;
    description?: string;
    icon?: string;
}
export type SearchBarSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
/** Variants aligned with Input component: primary, secondary, outline, ghost, filled */
export type SearchBarVariant = 'primary' | 'secondary' | 'outline' | 'ghost' | 'filled';
/** Color theme for border/focus ring; aligned with Input color option */
export type SearchBarColor = 'primary' | 'success' | 'warning' | 'danger' | 'info';
/** Label position: above the field (top) or inside the field, floats up on focus/fill (floating). Aligns with Input labelMode. */
export type SearchBarLabelMode = 'top' | 'floating';
export interface SearchBarProps {
    placeholder?: string;
    value: string;
    onChange: (query: string) => void;
    onSearch?: (query: string) => void;
    suggestions?: SearchSuggestion[];
    onSuggestionSelect?: (suggestion: SearchSuggestion) => void;
    isLoading?: boolean;
    size?: SearchBarSize;
    variant?: SearchBarVariant;
    /** Color theme for border and focus ring. Matches Input color option. Default 'primary'. */
    color?: SearchBarColor;
    /** Border radius; matches DS form controls (Input, Select). Default 'md' = rounded */
    radius?: 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
    className?: string;
    /** When true, show the default search icon on the left. When false, no left icon unless leftIcon is set. Default true. */
    showSearchIcon?: boolean;
    /** Custom content (e.g. icon) on the left. When set, used instead of the default search icon. */
    leftIcon?: React.ReactNode;
    /** Custom content (e.g. icon) on the right, shown before the loading/clear controls. */
    rightIcon?: React.ReactNode;
    /** When true, show the clear (close) button when value is non-empty. Default true. */
    isClearable?: boolean;
    noSuggestionsText?: string;
    /** When true, input is non-editable and suggestions are not shown. Matches DS disabled styling. */
    isDisabled?: boolean;
    /** Accessible name for the combobox (e.g. "Search addresses"). Falls back to placeholder for screen readers when not set. */
    ariaLabel?: string;
    /** Label text (e.g. "Search"). When set with showLabel, renders above or as floating label per labelMode. */
    label?: string;
    /** Whether to show the label. Default true when label is provided. */
    showLabel?: boolean;
    /** Label position: 'top' (above the field) or 'floating' (inside, floats up on focus/fill). Aligns with Input labelMode. */
    labelMode?: SearchBarLabelMode;
    /** Additional CSS classes for the label */
    labelClassName?: string;
    /** Id for the combobox input; used by label htmlFor when labelMode is 'top'. */
    id?: string;
    /** When false, the suggestions dropdown is never shown. Control when to show it (e.g. after 2+ chars: showSuggestionsList={value.trim().length >= 2}, or after API load). Default true. */
    showSuggestionsList?: boolean;
    /** When false, suggestions are shown as-is (no filtering by input). Use when the parent controls the list (e.g. API returns pre-filtered results). Default true. */
    filterSuggestions?: boolean;
}
export declare const SearchBar: React.FC<SearchBarProps>;
