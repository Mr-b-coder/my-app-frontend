import { default as React } from 'react';
import { VirtualizedTableSize, VirtualizedTableVariant, VirtualizedTableColor } from '../../types';
interface SortConfig {
    key: any;
    direction: 'asc' | 'desc';
    priority: number;
}
type CustomSortFunction<T> = (a: T, b: T, direction: 'asc' | 'desc') => number;
interface VirtualizedTableProps<T> {
    data: T[];
    columns: {
        key: any;
        header: string;
        sortable?: boolean;
        visible?: boolean;
        width?: number | 'auto' | 'fit-content';
        minWidth?: number;
        maxWidth?: number;
        textWrap?: 'wrap' | 'nowrap' | 'ellipsis';
        className?: string;
        render?: (value: any, row: T) => React.ReactNode;
        customSort?: CustomSortFunction<T>;
        headerIcon?: string;
        headerText?: string;
        headerRender?: (column: any) => React.ReactNode;
        headerClassName?: string;
        headerWidth?: number;
        showHeaderIcon?: boolean;
        showHeaderText?: boolean;
        headerActions?: React.ReactNode;
        filterable?: boolean;
        filterComponent?: React.ReactNode;
        resizable?: boolean;
        draggable?: boolean;
    }[];
    height: number;
    rowHeight: number;
    /** Additional CSS classes to apply to the table container */
    containerClassName?: string;
    /** Additional inline styles to apply to the table */
    style?: React.CSSProperties;
    /** The size of the table */
    size?: VirtualizedTableSize;
    /** The visual variant of the table */
    variant?: VirtualizedTableVariant;
    /** The color theme for the table */
    color?: VirtualizedTableColor;
    /** Whether to show the header */
    showHeader?: boolean;
    /** Whether to show the controls bar (search, filters, columns dropdown, etc.) @default false */
    showControls?: boolean;
    /** Custom header content */
    headerContent?: React.ReactNode;
    /** Additional CSS classes to apply to the header */
    headerClassName?: string;
    /** Whether the header has borders */
    headerBordered?: boolean;
    /** Whether to show column icons globally */
    showColumnIcons?: boolean;
    /** Whether to show column text globally */
    showColumnText?: boolean;
    /** Custom header padding */
    headerPadding?: 'none' | 'sm' | 'md' | 'lg' | 'xl';
    /** Additional CSS classes to apply to the body */
    bodyClassName?: string;
    /** Whether the body has borders */
    bodyBordered?: boolean;
    /** Whether to show the footer */
    showFooter?: boolean;
    /** Custom footer content */
    footerContent?: React.ReactNode;
    /** Additional CSS classes to apply to the footer */
    footerClassName?: string;
    /** Whether the footer has borders */
    footerBordered?: boolean;
    /** Whether the table has borders */
    bordered?: boolean;
    /** Additional CSS classes for custom styling */
    className?: string;
    /** Whether the table is searchable */
    searchable?: boolean;
    /** Placeholder text for search input */
    searchPlaceholder?: string;
    /** Whether the table is filterable */
    filterable?: boolean;
    /** Available filters */
    filters?: {
        key: string;
        label: string;
        value: string;
    }[];
    /** Callback when filter changes */
    onFilterChange?: (filterKey: string, value: string) => void;
    /** Currently active filter */
    activeFilter?: string;
    /** Whether rows are selectable */
    selectable?: boolean;
    /** The color theme for selected rows */
    selectionColor?: 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info';
    /** Callback when selection changes */
    onSelectionChange?: (selectedIds: string[]) => void;
    /** Whether to show the selection count info (e.g., "5 of 50 selected") @default false */
    showSelectionInfo?: boolean;
    /** Whether to show the virtualization info badge @default false */
    showVirtualizationInfo?: boolean;
    /** Whether rows are striped */
    striped?: boolean;
    /** Whether rows have hover effects */
    hoverable?: boolean;
    /** Table title */
    title?: string;
    /** Table description */
    description?: string;
    /** Whether to show row numbers */
    showRowNumbers?: boolean;
    /** Callback when row is clicked */
    onRowClick?: (row: T) => void;
    /** Whether the table is loading */
    loading?: boolean;
    /** Callback when an error occurs */
    onError?: (error: string) => void;
    /** Empty state configuration */
    emptyState?: {
        icon?: string;
        title: string;
        description: string;
    };
    /** Whether multi-column sorting is enabled */
    multiSort?: boolean;
    /** Default sort configuration */
    defaultSort?: SortConfig[];
    /** Callback when sort changes */
    onSortChange?: (sortConfig: SortConfig[]) => void;
    /** Whether the table is sortable */
    sortable?: boolean;
    /** Array of row IDs to highlight */
    highlightedRows?: (string | number)[];
    /** Array of column keys to highlight */
    highlightedColumns?: string[];
    /** Default highlight color */
    highlightColor?: 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'info';
    /** Whether the table has a background */
    hasBackground?: boolean;
    /** Whether the table has rounded corners */
    isRounded?: boolean;
    /** Whether the table has a shadow */
    hasShadow?: boolean;
    /** Whether the table is responsive */
    isResponsive?: boolean;
    /** Whether the table has a fixed width */
    hasFixedWidth?: boolean;
    /** Whether the table is full width */
    isFullWidth?: boolean;
    /** Whether the table has a fixed height */
    hasFixedHeight?: boolean;
    /** Whether the table is scrollable */
    isScrollable?: boolean;
    /** Whether the table has a sticky header */
    hasStickyHeader?: boolean;
    /** Whether the table has a sticky footer */
    hasStickyFooter?: boolean;
    /** Auto-size columns based on content */
    autoSizeColumns?: boolean;
    /** Minimum column width for auto-sizing */
    minColumnWidth?: number;
    /** Maximum column width for auto-sizing */
    maxColumnWidth?: number;
    /** Custom cell padding */
    cellPadding?: 'none' | 'sm' | 'md' | 'lg' | 'xl';
    /** Custom checkbox column width */
    checkboxColumnWidth?: number | 'auto';
    /** Custom row number column width */
    rowNumberColumnWidth?: number | 'auto';
}
export declare function VirtualizedTable<T extends {
    id: string | number;
}>({ data, columns, height, rowHeight, containerClassName, style, size, color, showHeader, showControls, headerContent, headerClassName, headerBordered, showColumnIcons, showColumnText, headerPadding, bodyClassName, bodyBordered, showFooter, footerClassName, footerBordered, bordered, className, searchable, searchPlaceholder, filterable, filters, onFilterChange, activeFilter, selectable, selectionColor, onSelectionChange, showSelectionInfo, showVirtualizationInfo, striped, hoverable, title, description, showRowNumbers, onRowClick, loading, onError, emptyState, multiSort, defaultSort, onSortChange, sortable, highlightedRows, highlightedColumns, highlightColor, hasBackground, isRounded, hasShadow, isResponsive, hasFixedWidth, isFullWidth, hasFixedHeight, isScrollable, hasStickyHeader, hasStickyFooter, autoSizeColumns, minColumnWidth, maxColumnWidth, cellPadding, checkboxColumnWidth, rowNumberColumnWidth }: VirtualizedTableProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
