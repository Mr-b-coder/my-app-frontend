import { default as React } from 'react';
import { CodeBlockSize, CodeBlockVariant } from '../../types';
/**
 * Props for the CodeBlock component
 */
export interface CodeBlockProps {
    /** The code content to display */
    code: string;
    /** The programming language for syntax highlighting */
    language?: string;
    /** The title or description of the code block */
    title?: string;
    /** Additional CSS classes to apply to the code block container */
    containerClassName?: string;
    /** The size of the code block */
    size?: CodeBlockSize;
    /** The visual variant of the code block */
    variant?: CodeBlockVariant;
    /** Whether the code block has line numbers */
    hasLineNumbers?: boolean;
    /** Whether the code block has a copy button */
    hasCopyButton?: boolean;
    /** Custom copy button text */
    copyButtonText?: string;
    /** Custom copied button text */
    copiedButtonText?: string;
    /** Callback when the code is copied */
    onCopy?: (code: string) => void;
    /** Additional props */
    [key: string]: any;
}
/**
 * A robust component for displaying code with syntax highlighting, line numbers, and a copy-to-clipboard button.
 */
export declare const CodeBlock: React.FC<CodeBlockProps>;
export default CodeBlock;
