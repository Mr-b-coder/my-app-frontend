import { default as React } from 'react';
/**
 * FileUploader size options
 */
export type FileUploaderSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
/**
 * FileUploader variant options
 */
export type FileUploaderVariant = 'default' | 'outlined' | 'filled' | 'minimal';
/**
 * FileUploader color options
 */
export type FileUploaderColor = 'primary' | 'success' | 'warning' | 'danger' | 'info';
/**
 * File validation error types
 */
export type FileValidationError = {
    file: File;
    errors: string[];
};
/**
 * Props for the FileUploader component
 */
export interface FileUploaderProps {
    /** The label text for the file uploader */
    label?: string;
    /** Unique identifier for the file uploader */
    id?: string;
    /** Currently selected files */
    files: File[];
    /** Callback when files change */
    onFilesChange: (files: File[]) => void;
    /** Whether multiple files can be selected */
    multiple?: boolean;
    /** Accepted file types (MIME types or extensions) */
    accept?: string;
    /** Maximum file size in bytes */
    maxSize?: number;
    /** Maximum number of files */
    maxFiles?: number;
    /** Whether the file uploader is disabled */
    disabled?: boolean;
    /** Whether the file uploader is required */
    required?: boolean;
    /** Error message to display */
    error?: string;
    /** Help text to display below the input */
    helpText?: string;
    /** The size of the file uploader */
    size?: FileUploaderSize;
    /** The variant of the file uploader */
    variant?: FileUploaderVariant;
    /** The color theme for the file uploader */
    color?: FileUploaderColor;
    /** Whether to show file previews */
    showPreview?: boolean;
    /** Whether to show upload progress */
    showProgress?: boolean;
    /** Whether to show file list */
    showFileList?: boolean;
    /** Whether to show drag instructions */
    showDragInstructions?: boolean;
    /** Custom placeholder text */
    placeholder?: string;
    /** Custom description text */
    description?: string;
    /** Additional CSS classes */
    className?: string;
    /** Additional inline styles */
    style?: React.CSSProperties;
    /** Callback when files are validated */
    onValidation?: (errors: FileValidationError[]) => void;
    /** Callback when upload fails */
    onUploadError?: (error: string) => void;
}
/**
 * FileUploader component for drag-and-drop file uploads
 *
 * @example
 * ```tsx
 * <FileUploader
 *   label="Upload Files"
 *   files={selectedFiles}
 *   onFilesChange={setSelectedFiles}
 *   multiple={true}
 *   accept="image/*,.pdf,.doc,.docx"
 *   maxSize={5 * 1024 * 1024} // 5MB
 *   showPreview={true}
 *   showProgress={true}
 * />
 * ```
 */
export declare const FileUploader: React.FC<FileUploaderProps>;
