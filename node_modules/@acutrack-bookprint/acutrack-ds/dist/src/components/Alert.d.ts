import { default as React } from 'react';
import { AlertSize, AlertVariant, AlertColor } from '../../types';
/**
 * Props for the Alert component
 */
export interface AlertProps {
    /** The main title text of the alert */
    title: string;
    /** The description text displayed below the title */
    description?: string;
    /** Additional content to display in the alert */
    children?: React.ReactNode;
    /** Additional CSS classes to apply to the alert container */
    containerClassName?: string;
    /** Additional CSS classes to apply to the alert */
    className?: string;
    /** Additional inline styles to apply to the alert */
    style?: React.CSSProperties;
    /** The size of the alert */
    size?: AlertSize;
    /** The visual style variant of the alert */
    variant?: AlertVariant;
    /** The color theme for the alert */
    color?: AlertColor;
    /** Whether the alert can be dismissed */
    isDismissible?: boolean;
    /** Whether the alert should have a border */
    isBordered?: boolean;
    /** Whether the alert should have rounded corners */
    isRounded?: boolean;
    /** Whether the alert should have a shadow */
    hasShadow?: boolean;
    /** Whether the alert should have an icon */
    hasIcon?: boolean;
    /** Custom icon to display in the alert */
    customIcon?: React.ReactNode;
    /** Whether the alert should have a close button */
    hasCloseButton?: boolean;
    /** Callback function when the alert is dismissed */
    onDismiss?: () => void;
    /** Whether the alert should have a loading state */
    isLoading?: boolean;
    /** Whether the alert should have a pulse animation */
    isPulsing?: boolean;
    /** Whether the alert should take full width of its container */
    isFullWidth?: boolean;
    /** Whether the alert should have a transparent background */
    isTransparent?: boolean;
    /** Whether the alert should have a gradient background */
    hasGradient?: boolean;
    /** Custom color for the alert */
    customColor?: string;
    /** Whether the alert should have a tooltip */
    hasTooltip?: boolean;
    /** Tooltip text for the alert */
    tooltipText?: string;
    /** Whether the alert should be animated when appearing */
    isAnimated?: boolean;
    /** Whether the alert should have a dismiss timeout */
    hasDismissTimeout?: boolean;
    /** Time in milliseconds before auto-dismissing the alert */
    dismissTimeout?: number;
    /** Whether the alert should have a progress bar */
    hasProgressBar?: boolean;
    /** Whether the alert should have a sound notification */
    hasSound?: boolean;
    /** Called once when the alert is shown and hasSound is true. Use to play a notification sound (e.g. via Web Audio API or an audio file). */
    onPlaySound?: () => void;
    /** Whether the alert should be persistent (not dismissible) */
    isPersistent?: boolean;
}
export declare const Alert: React.FC<AlertProps>;
