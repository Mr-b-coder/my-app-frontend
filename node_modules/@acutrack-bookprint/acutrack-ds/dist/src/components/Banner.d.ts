import { default as React } from 'react';
import { BannerSize, BannerVariant, BannerColor } from '../../types';
/**
 * Props for the Banner component
 */
export interface BannerProps {
    /** The content to display in the banner */
    children: React.ReactNode;
    /** Additional CSS classes to apply to the banner container */
    containerClassName?: string;
    /** Additional CSS classes to apply to the banner */
    className?: string;
    /** Additional inline styles to apply to the banner */
    style?: React.CSSProperties;
    /** The size of the banner */
    size?: BannerSize;
    /** The visual style variant of the banner */
    variant?: BannerVariant;
    /** The color theme for the banner */
    color?: BannerColor;
    /** Callback function when the banner is dismissed */
    onDismiss?: () => void;
    /** Custom icon to display in the banner */
    icon?: string;
    /** Action buttons or content to display on the right side */
    actions?: React.ReactNode;
    /** Whether the banner can be dismissed */
    isDismissible?: boolean;
    /** Whether the banner should have a border */
    isBordered?: boolean;
    /** Whether the banner should have rounded corners */
    isRounded?: boolean;
    /** Whether the banner should have a shadow */
    hasShadow?: boolean;
    /** Whether the banner should have an icon */
    hasIcon?: boolean;
    /** Whether the banner should have a close button */
    hasCloseButton?: boolean;
    /** Whether the banner should have a loading state */
    isLoading?: boolean;
    /** Whether the banner should have a pulse animation */
    isPulsing?: boolean;
    /** Whether the banner should take full width of its container */
    isFullWidth?: boolean;
    /** Whether the banner should have a transparent background */
    isTransparent?: boolean;
    /** Whether the banner should have a gradient background */
    hasGradient?: boolean;
    /** Custom color for the banner */
    customColor?: string;
    /** Whether the banner should have a tooltip */
    hasTooltip?: boolean;
    /** Tooltip text for the banner */
    tooltipText?: string;
    /** Whether the banner should be animated when appearing */
    isAnimated?: boolean;
    /** Whether the banner should have a dismiss timeout */
    hasDismissTimeout?: boolean;
    /** Time in milliseconds before auto-dismissing the banner */
    dismissTimeout?: number;
    /** Whether the banner should have a progress bar */
    hasProgressBar?: boolean;
    /** Whether the banner should have a sound notification */
    hasSound?: boolean;
    /** Whether the banner should be persistent (not dismissible) */
    isPersistent?: boolean;
    /** Whether the banner should have a title */
    hasTitle?: boolean;
    /** Title text for the banner */
    title?: string;
    /** Whether the banner should have a description */
    hasDescription?: boolean;
    /** Description text for the banner */
    description?: string;
}
export declare const Banner: React.FC<BannerProps>;
