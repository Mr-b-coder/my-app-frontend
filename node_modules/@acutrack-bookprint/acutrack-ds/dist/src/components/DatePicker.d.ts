import { default as React } from 'react';
import { PopoverPosition } from './Popover';
import { CalendarVariant, CalendarSize, CalendarColor, CalendarDensity, CalendarRadius, CalendarNavigation } from './Calendar';
import { DateFormat, DateFormatPreset, DateInput } from './date-helpers';
export type { DateFormat, DateFormatPreset };
/** Input field size options */
export type DatePickerSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
/** Input field visual variants - modern form input styles */
export type DatePickerInputVariant = 'outline' | 'filled' | 'underline' | 'flushed' | 'ghost';
/** Color theme options */
export type DatePickerColor = 'primary' | 'secondary' | 'success' | 'warning' | 'danger' | 'info' | 'neutral';
/** Label position modes */
export type DatePickerLabelMode = 'top' | 'floating' | 'inside' | 'inline' | 'none';
/** Border radius options */
export type DatePickerRadius = 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
/** Props for the DatePicker component */
export interface DatePickerProps {
    /** The currently selected date value */
    value: Date | null;
    /** Callback when the date selection changes */
    onChange: (date: Date | null) => void;
    /** The label text for the date picker */
    label?: string;
    /** Placeholder text when no date is selected */
    placeholder?: string;
    /** The unique identifier */
    id?: string;
    /**
     * Date format for display. Can be:
     * - Preset: 'short' | 'medium' | 'long' | 'full' | 'iso' | 'us' | 'eu' | 'uk' | 'numeric'
     * - Custom date-fns format string: 'dd/MM/yyyy', 'MMMM do, yyyy', etc.
     * - Custom function: (date: Date) => string
     * @default 'medium'
     */
    dateFormat?: DateFormat;
    /** Size of the input field */
    size?: DatePickerSize;
    /** Visual style of the input field */
    inputVariant?: DatePickerInputVariant;
    /** Color theme */
    color?: DatePickerColor;
    /** Label position mode */
    labelMode?: DatePickerLabelMode;
    /** Border radius */
    radius?: DatePickerRadius;
    /** Whether the picker is disabled */
    isDisabled?: boolean;
    /** Whether the picker is loading */
    isLoading?: boolean;
    /** Whether the picker has an error */
    isError?: boolean;
    /** Whether the picker shows success state */
    isSuccess?: boolean;
    /** Whether the field is required */
    isRequired?: boolean;
    /** Whether the picker is read-only */
    isReadOnly?: boolean;
    /** Whether the input takes full width */
    isFullWidth?: boolean;
    /** Show clear button when value is selected */
    hasClearButton?: boolean;
    /** Show calendar icon */
    hasCalendarIcon?: boolean;
    /** Show "Today" quick-select button in popover */
    hasTodayButton?: boolean;
    /** Show clear button in popover footer */
    hasPopoverClearButton?: boolean;
    /** Show required asterisk indicator */
    hasRequiredIndicator?: boolean;
    /** Helper text below the input */
    helperText?: string;
    /** Error message (shown when isError is true) */
    errorMessage?: string;
    /** Success message (shown when isSuccess is true) */
    successMessage?: string;
    /**
     * Minimum selectable date - accepts Date object or string
     * @example
     * minDate={new Date('2024-01-01')}      // Date object
     * minDate="2024-01-15"                   // ISO format
     * minDate="01/15/2024"                   // US format (MM/DD/YYYY)
     * minDate="15 Jan 2024"                  // UK format
     * minDate="January 15, 2024"             // Natural format
     */
    minDate?: DateInput;
    /**
     * Maximum selectable date - accepts Date object or string
     * @example
     * maxDate={new Date('2025-12-31')}      // Date object
     * maxDate="2025-12-31"                   // ISO format
     * maxDate="12/31/2025"                   // US format (MM/DD/YYYY)
     * maxDate="31 Dec 2025"                  // UK format
     * maxDate="December 31, 2025"            // Natural format
     */
    maxDate?: DateInput;
    /** Disabled specific dates */
    disabledDates?: Date[];
    /** Visual variant for the calendar dropdown */
    calendarVariant?: CalendarVariant;
    /** Calendar size (defaults to match input size) */
    calendarSize?: CalendarSize;
    /** Calendar density */
    calendarDensity?: CalendarDensity;
    /** Calendar border radius */
    calendarRadius?: CalendarRadius;
    /** Calendar navigation style */
    calendarNavigation?: CalendarNavigation;
    /** Show week numbers */
    showWeekNumber?: boolean;
    /** First day of week (0 = Sunday) */
    weekStartsOn?: 0 | 1 | 2 | 3 | 4 | 5 | 6;
    /** Start year for year dropdown */
    fromYear?: number;
    /** End year for year dropdown */
    toYear?: number;
    /** Close popover after date selection */
    closeOnSelect?: boolean;
    /** Popover position */
    position?: PopoverPosition;
    /** Called when picker opens */
    onOpen?: () => void;
    /** Called when picker closes */
    onClose?: () => void;
    /** Called when today is clicked */
    onTodayClick?: () => void;
    /** Called when clear is clicked */
    onClearClick?: () => void;
    /** Called on focus */
    onFocus?: (event: React.FocusEvent<HTMLButtonElement>) => void;
    /** Called on blur */
    onBlur?: (event: React.FocusEvent<HTMLButtonElement>) => void;
    /** Additional CSS class */
    className?: string;
    /** Additional inline styles */
    style?: React.CSSProperties;
    /** @deprecated Use inputVariant instead */
    variant?: 'primary' | 'secondary' | 'outline' | 'ghost' | 'text';
    /** @deprecated Use errorMessage instead */
    error?: string;
    /** @deprecated Use helperText instead */
    description?: string;
}
export declare const DatePicker: React.FC<DatePickerProps>;
export type { CalendarVariant, CalendarSize, CalendarColor, CalendarDensity, CalendarRadius, CalendarNavigation };
