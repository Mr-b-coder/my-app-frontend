import { default as React } from 'react';
import { BadgeSize, BadgeVariant, BadgeColor } from '../../types';
/**
 * Props for the Badge component
 */
export interface BadgeProps {
    /** The content to display inside the badge */
    children: React.ReactNode;
    /** Additional CSS classes to apply to the badge container */
    containerClassName?: string;
    /** Additional CSS classes to apply to the badge */
    className?: string;
    /** Additional inline styles to apply to the badge */
    style?: React.CSSProperties;
    /** The size of the badge */
    size?: BadgeSize;
    /** The visual style variant of the badge */
    variant?: BadgeVariant;
    /** The color theme for the badge */
    color?: BadgeColor;
    /** Whether the badge should have a border */
    isBordered?: boolean;
    /** Whether the badge should have rounded corners (deprecated, use radius instead) */
    isRounded?: boolean;
    /** The border radius of the badge */
    radius?: 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
    /** Whether the badge should have a shadow */
    hasShadow?: boolean;
    /** Whether the badge should be interactive (clickable) */
    isInteractive?: boolean;
    /** Callback function when the badge is clicked */
    onClick?: () => void;
    /** Whether the badge should be dismissible */
    isDismissible?: boolean;
    /** Callback function when the badge is dismissed */
    onDismiss?: () => void;
    /** Whether the badge should have an icon */
    hasIcon?: boolean;
    /** Icon to display in the badge */
    icon?: React.ReactNode;
    /** Whether the badge should have a pulse animation */
    isPulsing?: boolean;
    /** Whether the badge should have a loading state */
    isLoading?: boolean;
    /** Whether the badge should take full width of its container */
    isFullWidth?: boolean;
    /** Whether the badge should have a transparent background */
    isTransparent?: boolean;
    /** Whether the badge should have a gradient background */
    hasGradient?: boolean;
    /** Custom color for the badge */
    customColor?: string;
    /** Whether the badge should have a tooltip */
    hasTooltip?: boolean;
    /** Tooltip text for the badge */
    tooltipText?: string;
}
export declare const Badge: React.FC<BadgeProps>;
