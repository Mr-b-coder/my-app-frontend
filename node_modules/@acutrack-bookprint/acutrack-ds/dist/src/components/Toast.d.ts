import { default as React, ReactNode } from 'react';
export type ToastVariant = 'success' | 'error' | 'info' | 'warning' | 'danger';
export type ToastSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export type ToastPosition = 'top-left' | 'top-center' | 'top-right' | 'bottom-left' | 'bottom-center' | 'bottom-right';
interface ToastMessage {
    id: string;
    title: string;
    description?: string;
    variant: ToastVariant;
    size?: ToastSize;
}
export interface ToastProps {
    /** The toast message object */
    toast: ToastMessage;
    /** Callback when the toast is dismissed */
    onDismiss: (id: string) => void;
    /** The size of the toast */
    size?: ToastSize;
    /** Whether the toast has an icon */
    hasIcon?: boolean;
    /** Whether the toast has a close button */
    hasCloseButton?: boolean;
    /** Whether the toast has a title */
    hasTitle?: boolean;
    /** Whether the toast has a description */
    hasDescription?: boolean;
    /** Custom color for the toast */
    customColor?: string;
    /** Delay before auto-dismissing the toast */
    autoDismissDelay?: number;
    /** Whether the toast auto-dismisses */
    hasAutoDismiss?: boolean;
}
interface ToastContextType {
    showToast: (options: Omit<ToastMessage, 'id'>) => void;
    updatePosition: (position: ToastPosition) => void;
}
export interface ToastProviderProps {
    /** The children components */
    children: ReactNode;
    /** The default position for toasts */
    defaultPosition?: ToastPosition;
    /** The default size for toasts */
    defaultSize?: ToastSize;
    /** Delay before auto-dismissing toasts */
    autoDismissDelay?: number;
    /** Whether toasts auto-dismiss by default */
    hasAutoDismiss?: boolean;
    /** Whether toasts have an icon by default */
    hasIcon?: boolean;
    /** Whether toasts have a close button by default */
    hasCloseButton?: boolean;
    /** Whether toasts have a title by default */
    hasTitle?: boolean;
    /** Whether toasts have a description by default */
    hasDescription?: boolean;
    /** Custom color for toasts by default */
    customColor?: string;
}
export declare const useToast: () => ToastContextType;
export declare const Toast: React.FC<ToastProps>;
export declare const ToastProvider: React.FC<ToastProviderProps>;
export {};
