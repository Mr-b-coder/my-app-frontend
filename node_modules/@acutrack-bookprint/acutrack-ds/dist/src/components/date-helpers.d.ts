/** Preset date format options */
export type DateFormatPreset = 'short' | 'medium' | 'long' | 'full' | 'iso' | 'us' | 'eu' | 'uk' | 'numeric';
/** Custom format function type */
export type DateFormatFunction = (date: Date) => string;
/** Combined date format type - preset string, custom format string, or function */
export type DateFormat = DateFormatPreset | string | DateFormatFunction;
/**
 * Formats a date according to the specified format.
 *
 * @param date - The date to format
 * @param dateFormat - The format to use (preset, custom string, or function)
 * @param locale - The locale for formatting (default: 'en-US')
 * @returns Formatted date string
 *
 * @example
 * // Using presets
 * formatDateWithOptions(date, 'short')   // "1/6/26"
 * formatDateWithOptions(date, 'medium')  // "Jan 6, 2026"
 * formatDateWithOptions(date, 'long')    // "January 6, 2026"
 * formatDateWithOptions(date, 'full')    // "Monday, January 6, 2026"
 * formatDateWithOptions(date, 'iso')     // "2026-01-06"
 * formatDateWithOptions(date, 'us')      // "01/06/2026"
 * formatDateWithOptions(date, 'eu')      // "06/01/2026"
 *
 * @example
 * // Using date-fns format strings
 * formatDateWithOptions(date, 'dd/MM/yyyy')     // "06/01/2026"
 * formatDateWithOptions(date, 'MMMM do, yyyy')  // "January 6th, 2026"
 * formatDateWithOptions(date, 'EEE, MMM d')     // "Mon, Jan 6"
 *
 * @example
 * // Using custom function
 * formatDateWithOptions(date, (d) => `${d.getDate()}/${d.getMonth()+1}/${d.getFullYear()}`)
 */
export declare const formatDateWithOptions: (date: Date | null, dateFormat?: DateFormat, locale?: string) => string;
/**
 * Legacy format function - uses 'medium' preset
 * @deprecated Use formatDateWithOptions instead
 */
export declare const formatDate: (date: Date | null) => string;
export declare const dayNames: string[];
export declare const monthNames: string[];
export declare const isSameDay: (a: Date, b: Date) => boolean;
/**
 * Flexible date input type - accepts Date object or string
 * Supports various common date formats for developer convenience
 */
export type DateInput = Date | string;
/**
 * Parses a flexible date input into a Date object.
 * Supports multiple common date formats for developer convenience.
 *
 * @param input - Date object, string, or undefined/null
 * @returns Parsed Date object or undefined if invalid
 *
 * @example
 * // ISO format (recommended)
 * parseDateInput('2024-01-15')        // January 15, 2024
 * parseDateInput('2024-1-5')          // January 5, 2024
 *
 * @example
 * // US format (MM/DD/YYYY or M/D/YYYY)
 * parseDateInput('01/15/2024')        // January 15, 2024
 * parseDateInput('1/5/2024')          // January 5, 2024
 *
 * @example
 * // EU format (DD/MM/YYYY or D/M/YYYY) - use with 'eu' hint
 * parseDateInput('15/01/2024', 'eu')  // January 15, 2024
 *
 * @example
 * // UK format
 * parseDateInput('15 Jan 2024')       // January 15, 2024
 * parseDateInput('15-Jan-2024')       // January 15, 2024
 *
 * @example
 * // Natural format
 * parseDateInput('January 15, 2024')  // January 15, 2024
 * parseDateInput('Jan 15, 2024')      // January 15, 2024
 *
 * @example
 * // Date object passthrough
 * parseDateInput(new Date())          // Returns the same Date
 */
export declare const parseDateInput: (input: DateInput | undefined | null, formatHint?: "us" | "eu" | "auto") => Date | undefined;
export declare const getPresetRanges: () => {
    today: {
        start: Date;
        end: Date;
    };
    yesterday: {
        start: Date;
        end: Date;
    };
    thisWeek: {
        start: Date;
        end: Date;
    };
    last7Days: {
        start: Date;
        end: Date;
    };
    last30Days: {
        start: Date;
        end: Date;
    };
    thisMonth: {
        start: Date;
        end: Date;
    };
    lastMonth: {
        start: Date;
        end: Date;
    };
    thisQuarter: {
        start: Date;
        end: Date;
    };
    lastQuarter: {
        start: Date;
        end: Date;
    };
    thisYear: {
        start: Date;
        end: Date;
    };
};
/**
 * Common date-fns format patterns for reference:
 *
 * | Pattern | Result | Description |
 * |---------|--------|-------------|
 * | yyyy-MM-dd | 2026-01-06 | ISO format |
 * | MM/dd/yyyy | 01/06/2026 | US format |
 * | dd/MM/yyyy | 06/01/2026 | EU format |
 * | MMMM d, yyyy | January 6, 2026 | Full month |
 * | MMM d, yyyy | Jan 6, 2026 | Short month |
 * | EEE, MMM d | Mon, Jan 6 | Day of week |
 * | EEEE, MMMM do | Monday, January 6th | Full day + ordinal |
 * | d MMM yyyy | 6 Jan 2026 | UK format |
 * | do 'of' MMMM | 6th of January | Ordinal |
 * | yyyy/MM/dd | 2026/01/06 | Asian format |
 * | dd-MMM-yyyy | 06-Jan-2026 | Dash separated |
 */
export declare const dateFormatExamples: {
    iso: string;
    us: string;
    eu: string;
    uk: string;
    fullMonth: string;
    shortMonth: string;
    withDay: string;
    fullDay: string;
    ordinal: string;
    asian: string;
    dashSeparated: string;
};
