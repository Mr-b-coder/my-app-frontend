import { default as React } from 'react';
/**
 * Pagination size options
 */
export type PaginationSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
/**
 * Pagination variant options
 */
export type PaginationVariant = 'primary' | 'secondary' | 'text' | 'ghost' | 'outline' | 'warning' | 'danger';
/**
 * Pagination alignment options
 */
export type PaginationAlignment = 'left' | 'center' | 'right' | 'justify';
/**
 * Props for the Pagination component
 */
export interface PaginationProps {
    /** The current active page number */
    currentPage: number;
    /** The total number of pages */
    totalPages: number;
    /** Callback when a page is selected */
    onPageChange: (page: number) => void;
    /** The size of the pagination component */
    size?: PaginationSize;
    /** The variant of the pagination buttons */
    variant?: PaginationVariant;
    /** The alignment of the pagination component */
    alignment?: PaginationAlignment;
    /** Whether to show the previous/next buttons */
    hasNavigationButtons?: boolean;
    /** Whether to show page numbers */
    hasPageNumbers?: boolean;
    /** Whether to show ellipsis for skipped pages */
    hasEllipsis?: boolean;
    /** Whether to show the first/last page buttons */
    hasFirstLastButtons?: boolean;
    /** Whether to show page info (e.g., "Page 1 of 10") */
    hasPageInfo?: boolean;
    /** Whether to show items per page selector */
    hasItemsPerPageSelector?: boolean;
    /** Whether the pagination should be disabled */
    isDisabled?: boolean;
    /** Whether the pagination should have a loading state */
    isLoading?: boolean;
    /** Additional CSS classes to apply to the pagination component */
    className?: string;
    /** Additional inline styles to apply to the pagination component */
    style?: React.CSSProperties;
    /** Whether the pagination should have a background */
    hasBackground?: boolean;
    /** Whether the pagination should have borders */
    isBordered?: boolean;
    /** Whether the pagination should have rounded corners */
    isRounded?: boolean;
    /** Whether the pagination should have a shadow */
    hasShadow?: boolean;
    /** Whether the pagination should have a backdrop blur effect */
    hasBackdropBlur?: boolean;
    /** Whether the pagination should have a glass morphism effect */
    hasGlassMorphism?: boolean;
    /** Whether the pagination should have a minimum height */
    hasMinHeight?: boolean;
    /** Whether the pagination should be full width */
    isFullWidth?: boolean;
    /** Width of the pagination component */
    width?: 'sm' | 'md' | 'lg' | 'full' | 'auto' | 'none';
    /** Minimum width override */
    minWidth?: string;
    /** Maximum width override */
    maxWidth?: string;
    /** Whether the pagination should have an error state */
    isError?: boolean;
    /** Whether the pagination should have a warning state */
    isWarning?: boolean;
    /** Whether the pagination should have a success state */
    isSuccess?: boolean;
    /** Whether the pagination should have an info state */
    isInfo?: boolean;
    /** Whether the pagination should have hover effects */
    hasHoverEffect?: boolean;
    /** Whether the pagination should have focus effects */
    hasFocusEffect?: boolean;
    /** Whether the pagination should have active effects */
    hasActiveEffect?: boolean;
    /** Whether the pagination should be transparent */
    isTransparent?: boolean;
    /** Whether the pagination should have a gradient background */
    hasGradient?: boolean;
    /** Custom color for the pagination component */
    customColor?: string;
    /** Whether the pagination should have a tooltip */
    hasTooltip?: boolean;
    /** Text to display in the tooltip */
    tooltipText?: string;
    /** Whether the pagination should have loading text */
    hasLoadingText?: boolean;
    /** Text to display during loading state */
    loadingText?: string;
    /** Whether the pagination should have success text */
    hasSuccessText?: boolean;
    /** Text to display during success state */
    successText?: string;
    /** Whether the pagination should have warning text */
    hasWarningText?: boolean;
    /** Text to display during warning state */
    warningText?: string;
    /** Whether the pagination should have error text */
    hasErrorText?: boolean;
    /** Text to display during error state */
    errorText?: string;
    /** Whether the pagination should have info text */
    hasInfoText?: boolean;
    /** Text to display during info state */
    infoText?: string;
    /** Whether the pagination should have padding */
    hasPadding?: boolean;
    /** Whether the pagination should have margin */
    hasMargin?: boolean;
    /** Whether the pagination should be centered */
    isCentered?: boolean;
    /** Whether the pagination should have responsive behavior */
    isResponsive?: boolean;
    /** Whether the pagination should have a container */
    hasContainer?: boolean;
    /** Whether the pagination should have a max width */
    hasMaxWidth?: boolean;
    /** Whether the pagination should have a min width */
    hasMinWidth?: boolean;
    /** Whether the pagination should have a fixed height */
    hasFixedHeight?: boolean;
    /** Whether the pagination should have a fixed width */
    hasFixedWidth?: boolean;
    /** Whether the pagination should have a scrollable overflow */
    hasScrollableOverflow?: boolean;
    /** Whether the pagination should have a hidden overflow */
    hasHiddenOverflow?: boolean;
    /** Whether the pagination should have a visible overflow */
    hasVisibleOverflow?: boolean;
    /** Whether the pagination should have a clip overflow */
    hasClipOverflow?: boolean;
    /** Whether the pagination should have a scroll overflow */
    hasScrollOverflow?: boolean;
    /** Whether the pagination should have a auto overflow */
    hasAutoOverflow?: boolean;
    /** Whether the pagination should have a initial overflow */
    hasInitialOverflow?: boolean;
    /** Whether the pagination should have a inherit overflow */
    hasInheritOverflow?: boolean;
    /** Whether the pagination should have a unset overflow */
    hasUnsetOverflow?: boolean;
    /** Whether the pagination should have a revert overflow */
    hasRevertOverflow?: boolean;
    /** Whether the pagination should have a revert-layer overflow */
    hasRevertLayerOverflow?: boolean;
    /** Whether the pagination should have a label */
    hasLabel?: boolean;
    /** The label text for the pagination component */
    label?: string;
    /** Whether the label should be visually hidden */
    isLabelHidden?: boolean;
    /** Whether the pagination should have a description */
    hasDescription?: boolean;
    /** The description text for the pagination component */
    description?: string;
    /** Whether the description should be visually hidden */
    isDescriptionHidden?: boolean;
    /** Whether the pagination should have a help text */
    hasHelpText?: boolean;
    /** The help text for the pagination component */
    helpText?: string;
    /** Whether the help text should be visually hidden */
    isHelpTextHidden?: boolean;
    /** Whether the pagination should have a validation message */
    hasValidationMessage?: boolean;
    /** The validation message for the pagination component */
    validationMessage?: string;
    /** Whether the validation message should be visually hidden */
    isValidationMessageHidden?: boolean;
    /** Whether the pagination should have a required indicator */
    hasRequiredIndicator?: boolean;
    /** Whether the pagination is required */
    isRequired?: boolean;
    /** Whether the pagination should have a disabled state */
    hasDisabledState?: boolean;
    /** Whether the pagination should have a readonly state */
    hasReadonlyState?: boolean;
    /** Whether the pagination should have a loading state */
    hasLoadingState?: boolean;
    /** Whether the pagination should have a success state */
    hasSuccessState?: boolean;
    /** Whether the pagination should have a warning state */
    hasWarningState?: boolean;
    /** Whether the pagination should have an error state */
    hasErrorState?: boolean;
    /** Whether the pagination should have an info state */
    hasInfoState?: boolean;
    /** Whether the pagination should have a neutral state */
    hasNeutralState?: boolean;
    /** Whether the pagination should have a primary state */
    hasPrimaryState?: boolean;
    /** Whether the pagination should have a secondary state */
    hasSecondaryState?: boolean;
    /** Whether the pagination should have a tertiary state */
    hasTertiaryState?: boolean;
    /** Whether the pagination should have a quaternary state */
    hasQuaternaryState?: boolean;
    /** Whether the pagination should have a quinary state */
    hasQuinaryState?: boolean;
    /** Whether the pagination should have a senary state */
    hasSenaryState?: boolean;
    /** Whether the pagination should have a septenary state */
    hasSeptenaryState?: boolean;
    /** Whether the pagination should have a octonary state */
    hasOctonaryState?: boolean;
    /** Whether the pagination should have a nonary state */
    hasNonaryState?: boolean;
    /** Whether the pagination should have a denary state */
    hasDenaryState?: boolean;
    /** Callback when the pagination component is clicked */
    onClick?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the pagination component receives focus */
    onFocus?: (event: React.FocusEvent<HTMLElement>) => void;
    /** Callback when the pagination component loses focus */
    onBlur?: (event: React.FocusEvent<HTMLElement>) => void;
    /** Callback when the mouse enters the pagination component */
    onMouseEnter?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the mouse leaves the pagination component */
    onMouseLeave?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the pagination component is pressed */
    onMouseDown?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the pagination component is released */
    onMouseUp?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the pagination component is double-clicked */
    onDoubleClick?: (event: React.MouseEvent<HTMLElement>) => void;
    /** Callback when the pagination component receives a key press */
    onKeyDown?: (event: React.KeyboardEvent<HTMLElement>) => void;
    /** Callback when the pagination component receives a key release */
    onKeyUp?: (event: React.KeyboardEvent<HTMLElement>) => void;
    /** Callback when the pagination component receives a key press */
    onKeyPress?: (event: React.KeyboardEvent<HTMLElement>) => void;
    /** Callback when the previous page button is clicked */
    onPreviousPage?: () => void;
    /** Callback when the next page button is clicked */
    onNextPage?: () => void;
    /** Callback when the first page button is clicked */
    onFirstPage?: () => void;
    /** Callback when the last page button is clicked */
    onLastPage?: () => void;
    /** Callback when items per page selection changes */
    onItemsPerPageChange?: (itemsPerPage: number) => void;
}
/**
 * Pagination component for navigating through paginated content
 *
 * @example
 * ```tsx
 * <Pagination
 *   currentPage={1}
 *   totalPages={10}
 *   onPageChange={handlePageChange}
 *   size="md"
 *   variant="primary"
 *   hasNavigationButtons
 *   hasPageNumbers
 *   hasEllipsis
 * />
 * ```
 */
export declare const Pagination: React.FC<PaginationProps>;
