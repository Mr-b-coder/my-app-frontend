import { default as React } from 'react';
/**
 * Props for the Spinner component
 */
export type SpinnerProps = {
    /** The size of the spinner */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
    /** The animation variant of the spinner */
    variant?: 'ring' | 'dots' | 'bars' | 'grid' | 'pulsar' | 'dual-ring' | 'logo';
    /** The color of the spinner */
    color?: 'primary' | 'white' | 'current' | 'success' | 'warning' | 'error' | 'info';
    /** Additional CSS classes to apply to the spinner */
    className?: string;
    /** Whether the spinner should have a border */
    isBordered?: boolean;
    /** Whether the spinner should have rounded corners */
    isRounded?: boolean;
    /** Whether the spinner should have a shadow */
    hasShadow?: boolean;
    /** Whether the spinner should have a background */
    hasBackground?: boolean;
    /** Whether the spinner should have a focus ring */
    hasFocusRing?: boolean;
    /** Whether the spinner should be interactive */
    isInteractive?: boolean;
    /** Whether the spinner should have a pulse animation */
    isPulsing?: boolean;
    /** Whether the spinner is loading */
    loading?: boolean;
    /** Whether the spinner should take full width of its container */
    fullWidth?: boolean;
    /** Whether the spinner should have a gradient background */
    hasGradient?: boolean;
    /** Custom color for the spinner */
    customColor?: string;
    /** Whether the spinner should have a tooltip */
    hasTooltip?: boolean;
    /** Tooltip text for the spinner */
    tooltipText?: string;
    /** Whether the spinner should be animated */
    isAnimated?: boolean;
    /** Animation duration in seconds */
    animationDuration?: number;
    /** Whether the spinner should have a label */
    hasLabel?: boolean;
    /** Label text for the spinner */
    label?: string;
    /** Whether the spinner should have a description */
    hasDescription?: boolean;
    /** Description text for the spinner */
    description?: string;
    /** Whether the spinner should have a success state */
    isSuccess?: boolean;
    /** Whether the spinner should have a warning state */
    isWarning?: boolean;
    /** Whether the spinner should have an error state */
    isError?: boolean;
    /** Whether the spinner should have an info state */
    isInfo?: boolean;
    /** Whether the spinner should have a custom size */
    hasCustomSize?: boolean;
    /** Custom width for the spinner */
    customWidth?: string;
    /** Custom height for the spinner */
    customHeight?: string;
    /** Whether the spinner should have a border radius */
    hasBorderRadius?: boolean;
    /** Whether the spinner should have a border style */
    hasBorderStyle?: boolean;
    /** Border style for the spinner */
    borderStyle?: 'solid' | 'dashed' | 'dotted';
    /** Whether the spinner should have a border width */
    hasBorderWidth?: boolean;
    /** Border width for the spinner */
    borderWidth?: string;
    /** Whether the spinner should have a border color */
    hasBorderColor?: boolean;
    /** Border color for the spinner */
    borderColor?: string;
    /** Whether the spinner should have a background color */
    hasBackgroundColor?: boolean;
    /** Background color for the spinner */
    backgroundColor?: string;
    /** Whether the spinner should have a text color */
    hasTextColor?: boolean;
    /** Text color for the spinner */
    textColor?: string;
    /** Whether the spinner should have a hover effect */
    hasHoverEffect?: boolean;
    /** Whether the spinner should have a focus effect */
    hasFocusEffect?: boolean;
    /** Whether the spinner should have an active effect */
    hasActiveEffect?: boolean;
    /** Whether the spinner is disabled */
    disabled?: boolean;
    /** Whether the spinner should have a loading text */
    hasLoadingText?: boolean;
    /** Loading text for the spinner */
    loadingText?: string;
    /** Whether the spinner should have a success text */
    hasSuccessText?: boolean;
    /** Success text for the spinner */
    successText?: string;
    /** Whether the spinner should have a warning text */
    hasWarningText?: boolean;
    /** Warning text for the spinner */
    warningText?: string;
    /** Whether the spinner should have an error text */
    hasErrorText?: boolean;
    /** Error text for the spinner */
    errorText?: string;
    /** Whether the spinner should have an info text */
    hasInfoText?: boolean;
    /** Info text for the spinner */
    infoText?: string;
};
export declare const Spinner: React.FC<SpinnerProps>;
