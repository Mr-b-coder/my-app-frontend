import { default as React } from 'react';
export interface FormErrorBoundaryProps {
    /** The error that occurred */
    error: Error;
    /** Function to reset the error state */
    resetError: () => void;
    /** The size of the error boundary */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
    /** The visual variant of the error boundary */
    variant?: 'default' | 'minimal' | 'elevated' | 'bordered' | 'filled';
    /** The color theme for the error boundary */
    color?: 'primary' | 'success' | 'warning' | 'danger' | 'info' | 'neutral';
    /** Whether to preserve form data when an error occurs */
    preserveFormData?: boolean;
    /** Whether to show form recovery options */
    showRecoveryOptions?: boolean;
    /** Message to display about form recovery */
    recoveryMessage?: string;
    /** Whether to show form backup options */
    showBackupOptions?: boolean;
    /** Whether to show form export options */
    showExportOptions?: boolean;
    /** Whether to show form reset options */
    showResetOptions?: boolean;
    /** The content to display inside the error boundary */
    children: React.ReactNode;
    /** Additional CSS classes for the error boundary */
    className?: string;
    /** Additional inline styles to apply to the error boundary */
    style?: React.CSSProperties;
}
/**
 * FormErrorBoundary component that catches form-related errors
 */
export declare const FormErrorBoundary: React.FC<FormErrorBoundaryProps>;
