import { default as React } from 'react';
import { AccordionSize, AccordionVariant, AccordionColor } from '../../types';
/**
 * Props for the Accordion component
 */
interface AccordionProps {
    /** The title text displayed in the accordion header */
    title: string;
    /** The content to display inside the accordion */
    children: React.ReactNode;
    /** Additional CSS classes to apply to the accordion container */
    containerClassName?: string;
    /** Additional CSS classes to apply to the accordion header */
    headerClassName?: string;
    /** Additional CSS classes to apply to the accordion content */
    contentClassName?: string;
    /** Additional inline styles to apply to the accordion */
    style?: React.CSSProperties;
    /** The size of the accordion */
    size?: AccordionSize;
    /** The visual variant of the accordion */
    variant?: AccordionVariant;
    /** The color theme for the accordion */
    color?: AccordionColor;
    /** Whether the accordion should be open by default */
    isOpenByDefault?: boolean;
    /** Whether the accordion should have a border */
    isBordered?: boolean;
    /** Whether the accordion should have rounded corners */
    isRounded?: boolean;
    /** Whether the accordion should have a shadow */
    hasShadow?: boolean;
    /** Whether the accordion should be disabled */
    isDisabled?: boolean;
    /** Whether the accordion should have an icon */
    hasIcon?: boolean;
    /** Whether the accordion should have smooth animations */
    isAnimated?: boolean;
    /** Whether the accordion should take full width */
    isFullWidth?: boolean;
    /** Whether the accordion should have a loading state */
    isLoading?: boolean;
    /** Whether the accordion should have a custom icon */
    customIcon?: React.ReactNode;
    /** Whether the accordion should have a tooltip */
    hasTooltip?: boolean;
    /** Tooltip text for the accordion */
    tooltipText?: string;
    /** Callback when accordion state changes */
    onToggle?: (isOpen: boolean) => void;
}
export declare const Accordion: React.FC<AccordionProps>;
export {};
