import { default as React } from 'react';
export type CircularProgressSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export type CircularProgressColor = 'primary' | 'success' | 'warning' | 'danger' | 'info';
export interface CircularProgressProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'children'> {
    /** The current progress value (0-100). If omitted, the progress becomes indeterminate (spinner). */
    value?: number;
    /** The minimum value. @default 0 */
    min?: number;
    /** The maximum value. @default 100 */
    max?: number;
    /** The size of the circular progress. @default 'md' */
    size?: CircularProgressSize;
    /** The color theme. @default 'primary' */
    color?: CircularProgressColor;
    /** The thickness of the progress stroke in pixels. @default 4 */
    thickness?: number;
    /** Whether to show the percentage text in the center. @default false */
    showPercentage?: boolean;
    /** Custom content to display in the center (overrides showPercentage). */
    children?: React.ReactNode;
    /** Custom CSS classes for the container. */
    className?: string;
    /** Custom CSS classes for the track (background circle). */
    trackClassName?: string;
    /** Custom CSS classes for the progress circle. */
    progressClassName?: string;
}
export declare const CircularProgress: React.FC<CircularProgressProps>;
