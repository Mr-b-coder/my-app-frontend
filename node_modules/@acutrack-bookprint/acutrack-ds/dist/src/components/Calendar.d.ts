import { default as React } from 'react';
export type CalendarSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export type CalendarColor = 'primary' | 'secondary' | 'success' | 'warning' | 'danger' | 'info' | 'neutral';
export type CalendarVariant = 'solid' | 'soft' | 'outline' | 'minimal' | 'glass';
export type CalendarDensity = 'compact' | 'comfortable' | 'spacious' | 'relaxed';
export type CalendarRadius = 'none' | 'sm' | 'md' | 'lg' | 'xl' | 'full';
export type CalendarNavigation = 'dropdown' | 'buttons' | 'modern';
/** Selection mode: 'single' for one date, 'range' for start/end, 'multiple' for many dates */
export type CalendarMode = 'single' | 'range' | 'multiple';
/** Date range type for range mode */
export interface DateRange {
    from: Date | undefined;
    to?: Date | undefined;
}
interface CalendarBaseProps {
    displayMonth?: Date;
    onDisplayMonthChange?: (month: Date) => void;
    className?: string;
    size?: CalendarSize;
    color?: CalendarColor;
    variant?: CalendarVariant;
    density?: CalendarDensity;
    radius?: CalendarRadius;
    disabled?: boolean;
    showOutsideDays?: boolean;
    showWeekNumber?: boolean;
    showHeader?: boolean;
    highlightToday?: boolean;
    /** Navigation style: 'dropdown' (native select), 'buttons' (arrows only), 'modern' (popover grid) */
    navigation?: CalendarNavigation;
    /** Number of months to display side by side (default: 1, use 2 for dual calendar) */
    numberOfMonths?: number;
    /** When true with numberOfMonths > 1, each calendar gets independent navigation arrows */
    independentNavigation?: boolean;
    fromDate?: Date;
    toDate?: Date;
    fromYear?: number;
    toYear?: number;
    disabledDates?: Date[];
    weekStartsOn?: 0 | 1 | 2 | 3 | 4 | 5 | 6;
    elevated?: boolean;
    bordered?: boolean;
    fullWidth?: boolean;
    animated?: boolean;
    /** @deprecated Use `value` instead. */
    selectedValue?: Date | null;
    /** @deprecated Use `displayMonth` instead. */
    displayDate?: Date;
    /** @deprecated Use `onDisplayMonthChange` instead. */
    onDisplayDateChange?: (d: Date) => void;
    /** @deprecated Use `elevated` instead. */
    hasShadow?: boolean;
    /** @deprecated Use `bordered` instead. */
    isBordered?: boolean;
}
interface CalendarSingleProps extends CalendarBaseProps {
    mode?: 'single';
    value?: Date;
    onDateSelect?: (date: Date | undefined) => void;
    rangeValue?: never;
    onRangeSelect?: never;
    multiValue?: never;
    onMultiSelect?: never;
    /** Minimum selectable dates for range/multiple */
    min?: never;
    /** Maximum selectable dates for range/multiple */
    max?: never;
}
interface CalendarRangeProps extends CalendarBaseProps {
    mode: 'range';
    /** Selected date range */
    rangeValue?: DateRange;
    /** Callback when range is selected */
    onRangeSelect?: (range: DateRange | undefined) => void;
    /** Minimum number of days in range */
    min?: number;
    /** Maximum number of days in range */
    max?: number;
    /**
     * Allow adjusting range ends after selection (click to extend/shrink range).
     * Default: false (industry-standard behavior where third click starts a new range)
     * When true: Clicking after a complete range will modify the existing range instead of starting fresh.
     */
    adjustable?: boolean;
    /** Allow dragging range ends to adjust the selection */
    draggable?: boolean;
    value?: never;
    onDateSelect?: never;
    multiValue?: never;
    onMultiSelect?: never;
}
interface CalendarMultipleProps extends CalendarBaseProps {
    mode: 'multiple';
    /** Array of selected dates */
    multiValue?: Date[];
    /** Callback when selection changes */
    onMultiSelect?: (dates: Date[] | undefined) => void;
    /** Minimum number of dates that can be selected */
    min?: number;
    /** Maximum number of dates that can be selected */
    max?: number;
    value?: never;
    onDateSelect?: never;
    rangeValue?: never;
    onRangeSelect?: never;
}
export type CalendarProps = CalendarSingleProps | CalendarRangeProps | CalendarMultipleProps;
export declare const Calendar: React.FC<CalendarProps>;
export default Calendar;
