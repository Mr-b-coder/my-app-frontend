import { default as React, RefObject } from 'react';
export declare const useOnClickOutside: (ref: RefObject<HTMLElement | null>, handler: (event: MouseEvent | TouchEvent) => void) => void;
export declare const useCopyToClipboard: () => [(text: string) => void, boolean];
export interface IconProps {
    /** The Material Symbol name (e.g., 'home', 'settings', 'info') */
    children: string;
    /** Additional CSS classes to apply */
    className?: string;
    /** Size of the icon - can be a Tailwind size class or custom value */
    size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl' | '3xl' | string;
    /** Whether the icon should be filled (1) or outlined (0) */
    filled?: boolean;
    /** Font weight for the icon (100-700) */
    weight?: 100 | 200 | 300 | 400 | 500 | 600 | 700;
    /** Optical size for the icon */
    opticalSize?: number;
    /** Whether to hide the icon from screen readers (for decorative icons) */
    ariaHidden?: boolean;
    /** Accessible label for the icon (required if not ariaHidden) */
    ariaLabel?: string;
    /** Color variant for the icon */
    color?: 'primary' | 'secondary' | 'tertiary' | 'success' | 'warning' | 'error' | 'info' | 'brand-navy' | 'brand-teal' | 'inverse' | 'current';
    /** Whether the icon should have a subtle animation on hover */
    hasHoverEffect?: boolean;
    /** Whether the icon should be clickable */
    clickable?: boolean;
}
export declare const Icon: React.FC<IconProps>;
