import { default as React } from 'react';
import { SelectOption } from './Select';
import { DropdownPosition } from './Dropdown';
/**
 * MultiSelect size options
 */
export type MultiSelectSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
/**
 * MultiSelect variant options
 */
export type MultiSelectVariant = 'primary' | 'secondary' | 'outline' | 'ghost' | 'filled';
/**
 * MultiSelect color options
 */
export type MultiSelectColor = 'primary' | 'success' | 'warning' | 'danger' | 'info';
/** Label position mode: above the field (top) or inside the field, floating up on focus/fill (floating) */
export type MultiSelectLabelMode = 'top' | 'floating';
/**
 * Props for the MultiSelect component
 */
export interface MultiSelectProps {
    /** The label text for the multi-select component */
    label?: string;
    /** The unique identifier for the multi-select component */
    id?: string;
    /** The available options to choose from */
    options: SelectOption[];
    /** The currently selected values */
    value: (string | number)[];
    /** Callback when selected values change */
    onChange: (values: (string | number)[]) => void;
    /** Placeholder text when no options are selected */
    placeholder?: string;
    /** Whether the multi-select should be disabled */
    disabled?: boolean;
    /** Whether the multi-select should be required */
    required?: boolean;
    /** Error message to display */
    error?: string;
    /** Help text to display below the input */
    helpText?: string;
    /** The size of the multi-select component */
    size?: MultiSelectSize;
    /** The variant of the multi-select component */
    variant?: MultiSelectVariant;
    /** The color theme of the multi-select component */
    color?: MultiSelectColor;
    /** The position of the dropdown relative to the trigger */
    position?: DropdownPosition;
    /** Whether to show a search input */
    searchable?: boolean;
    /** Label position: 'top' (above the field) or 'floating' (inside the field, floats up on focus/open/fill). Aligns with DatePicker labelMode. */
    labelMode?: MultiSelectLabelMode;
    /** Whether to show selected items as chips/tags */
    showChips?: boolean;
    /** Whether to show a clear all button */
    clearable?: boolean;
    /** Whether to show a select all button */
    selectAll?: boolean;
    /** Whether the multi-select should have a loading state */
    loading?: boolean;
    /** Maximum number of items that can be selected */
    maxSelections?: number;
    /** Whether checkboxes can be disabled */
    disableCheckboxes?: boolean;
    /** Custom search function */
    onSearch?: (searchTerm: string) => void;
    /** Callback when an option is selected */
    onSelect?: (option: SelectOption) => void;
    /** Callback when an option is deselected */
    onDeselect?: (option: SelectOption) => void;
    /** Additional CSS classes to apply to the multi-select component */
    className?: string;
    /** Additional inline styles to apply to the multi-select component */
    style?: React.CSSProperties;
    /** ARIA label for accessibility */
    'aria-label'?: string;
    /** ARIA described by for accessibility */
    'aria-describedby'?: string;
    /** ARIA invalid for accessibility */
    'aria-invalid'?: boolean;
    /** Maximum height of the dropdown. Default: '15rem' */
    maxDropdownHeight?: string | number;
    /** Line height for dropdown items */
    itemLineHeight?: string | number;
}
/**
 * MultiSelect component for selecting multiple options from a list
 *
 * @example
 * ```tsx
 * <MultiSelect
 *   label="Select Categories"
 *   options={categoryOptions}
 *   value={selectedCategories}
 *   onChange={setSelectedCategories}
 *   placeholder="Choose categories..."
 *   searchable
 *   showChips
 *   clearable
 * />
 * ```
 */
export declare const MultiSelect: React.FC<MultiSelectProps>;
