# Authenticate to npm registry for @acutrack-bookprint (used on Netlify via NPM_TOKEN env var)
# Do NOT put your token in this file. Add NPM_TOKEN in Netlify → Site configuration → Environment variables.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
