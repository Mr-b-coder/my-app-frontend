// This component is no longer used and has been replaced by InteractiveInteriorSetup.tsx
// Keeping the file to prevent import errors during transition, but its content is effectively deprecated.
import React from 'react';

const DeprecatedInteriorPageSetup: React.FC = () => {
  return null; 
};

export default DeprecatedInteriorPageSetup;